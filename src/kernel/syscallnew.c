#include "debug.h"
#include "common.h"

// oscomp syscalls that haven't been implemented

uint64 oscomp_sys_getdents64(void) {
	ASSERT(0);
	return 0;
}



uint64 oscomp_sys_linkat(void) {
	ASSERT(0);
	return 0;
}


uint64 oscomp_sys_umount2(void) {
	ASSERT(0);
	return 0;
}

uint64 oscomp_sys_mount(void) {
	ASSERT(0);
	return 0;
}


uint64 oscomp_sys_clone(void) {
	ASSERT(0);
	return 0;
}

uint64 oscomp_sys_execve(void) {
	ASSERT(0);
	return 0;
}

uint64 oscomp_sys_wait4(void) {
	ASSERT(0);
	return 0;
}

uint64 oscomp_sys_exit(void) {
	ASSERT(0);
	return 0;
}

uint64 oscomp_sys_getppid(void) {
	ASSERT(0);
	return 0;
}

uint64 oscomp_sys_getpid(void) {
	ASSERT(0);
	return 0;
}

uint64 oscomp_sys_brk(void) {
	ASSERT(0);
	return 0;
}

uint64 oscomp_sys_munmap(void) {
	ASSERT(0);
	return 0;
}

uint64 oscomp_sys_mmap(void) {
	ASSERT(0);
	return 0;
}

uint64 oscomp_sys_times(void) {
	ASSERT(0);
	return 0;
}

uint64 oscomp_sys_uname(void) {
	ASSERT(0);
	return 0;
}

uint64 oscomp_sys_sched_yield(void) {
	ASSERT(0);
	return 0;
}

uint64 oscomp_sys_gettimeofday(void) {
	ASSERT(0);
	return 0;
}

uint64 oscomp_sys_nanosleep(void) {
	ASSERT(0);
	return 0;
}

